version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: ./frontend/Dockerfile
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - mern-network

  backend:
    build:
      context: .
      dockerfile: ./backend/Dockerfile
    environment:
      - MONGO_URI=mongodb+srv://23it087_db_user:89uJ8oso6i5e25tm@cluster0.czibetv.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
    ports:
      - "5000:5000"
    depends_on:
      - mongo
    networks:
      - mern-network

  mongo:
    image: mongo:latest
    volumes:
      - mongo-data:/data/db
    networks:
      - mern-network

networks:
  mern-network:
    driver: bridge

volumes:
  mongo-data:
    driver: local
